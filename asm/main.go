package main

// Importamos la función ensamblador
func HolaMundo()

// Definimos la función printf en Go (requiere que esté enlazada con libc)
func printf(format *byte, args ...interface{})

func main() {
	HolaMundo() // Llamamos la función ensamblador
}
